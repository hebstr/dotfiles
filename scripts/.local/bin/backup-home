#!/usr/bin/env bash
set -euo pipefail

REMOTE="cpd000001"
SOURCE="${REMOTE}:/home/edjulien/"
DEST="/s/julien.elicesdiez/backup/edjulien/"
EXCLUDES="${HOME}/.config/backup/excludes.txt"

rclone sync \
  "${SOURCE}" \
  "${DEST}" \
  --filter-from="${EXCLUDES}" \
  --progress
