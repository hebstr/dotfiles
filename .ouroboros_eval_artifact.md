A documentation file (\~/.claude/rules/python.md) instructs Claude to branch on a specific line of tool output.
It calls that line "the tell" and "the branch selector, and the only reliable one", and the decision rule is: if the line is present, re-run the tool with a different flag and use that result; if absent, accept the first result.

THE TWO PLACES THE DOCUMENTATION QUOTES THAT LINE:

Quotation 1: `using preset basic`

Quotation 2: `No pyrefly.toml found — using preset basic.`

THE ACTUAL TOOL OUTPUT, captured verbatim from stderr (pyrefly 1.1.1), reproduced here between the markers:

<<<BEGIN
No `pyrefly.toml` found --- using preset `basic`.
END>>>

For reference, a shell check performed against the real output:
`pyrefly check bad.py 2>&1 | grep -c 'using preset basic'` returns 0
`pyrefly check bad.py 2>&1 | grep -c 'using preset`basic`'` returns 1

Compare Quotation 1 and Quotation 2 against the actual output and report whether each reproduces it exactly.
