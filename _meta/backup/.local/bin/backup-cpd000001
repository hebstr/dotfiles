#!/usr/bin/env bash
set -euo pipefail

HOST="cpd000001"
REPO="edjulien/"
SOURCE="$HOST:/home/$REPO"
DEST="/s/julien.elicesdiez/backup/$REPO"
EXCLUDES="$HOME/.config/backup/excludes.txt"

rclone sync "$SOURCE" "$DEST" \
  --filter-from="$EXCLUDES" \
  --delete-excluded \
  --progress
