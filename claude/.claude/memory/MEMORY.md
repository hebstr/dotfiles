# Memory Index

## User
- [user_profile.md](user_profile.md): Environment, accounts, and context not in CLAUDE.md
- [user_quarto_typst_only.md](user_quarto_typst_only.md): Quarto PDF via Typst only; user does not use LaTeX (do not suggest the `format: pdf` LaTeX path)

## Project
- [project_opencode_agents_md_mirror.md](project_opencode_agents_md_mirror.md): opencode's global `AGENTS.md` is a hand-picked digest of CLAUDE.md's behavioral rules; after changing such a rule in CLAUDE.md, update or propose it there in the same response
- [project_ju_tp2_receiveonly.md](project_ju_tp2_receiveonly.md): On ju-TP2 (WSL) never write to `~/dotfiles` or `~/.claude`: Syncthing receive-only there, ju-TP is the single writer
- [project_ubuntu_26_04_migration.md](project_ubuntu_26_04_migration.md): Migration 24.04 → 26.04 différée à décembre 2026 au plus tôt (décision 2026-09-09); déclencheur = `Supported: 1` dans le meta-release Canonical; recherche complète dans `~/dotfiles/_meta/notes/ubuntu-26-04-migration-reco.md`, ne pas la refaire
- [project_quarto_custom_crossref_float.md](project_quarto_custom_crossref_float.md): Custom crossref float types (`anx`): no native appendix prefix, knitr labels limited to `fig-`/`tbl-`, the working `pre-quarto` `FloatRefTarget` filter that retypes a `tbl-anx-` float, the `<ref>-cap` and per-type-numbering limits, and the two routes abandoned (R helper via `as_raw_html`, knitr `label` hook)
- [project_quarto_extensions_need_project_root.md](project_quarto_extensions_need_project_root.md): Without a `_quarto.yml` there is no project, so `_extensions` resolves only in the input file's own directory: a `.qmd` using a custom format must stay beside it
- [project_qmd_format_hook.md](project_qmd_format_hook.md): .qmd/.md formatting with panache and its prek gate: config replacement and exclude gotchas, the CLI/LSP split on format-on-save, and the constructs panache damages
- [project_review_workflow_backlog.md](project_review_workflow_backlog.md): Implementation backlog from the 2026-03 code review glowup audit (hebstr CI, data validation)
- [project_rv_install_deployment.md](project_rv_install_deployment.md): rv-update and quarto-update are stored in dotfiles but deployed on multi-user servers; affects threat model and portability (no amd64 assumption)
- [project_shellharden_dotfiles_normalized.md](project_shellharden_dotfiles_normalized.md): Decision reversed 2026-09-12: `bin/.local/bin/`, `claude/.claude/hooks/` and (since 2026-09-19) `bash/` are shellharden-clean, the `$VAR` / `[ "$x" != "" ]` style there is shellharden's output and must not be reverted; a `shellharden --check` prek hook blocks the drift at commit, in `prek.toml` and in the `_meta/profiles/prek.toml` template alike
- [project_litrev_separate_from_hebstr.md](project_litrev_separate_from_hebstr.md): Decision (2026-04-26): keep litrev separate from hebstr marketplace; do not re-propose merge
- [project_hebstr_doc_adaptive_figures.md](project_hebstr_doc_adaptive_figures.md): Theme-adaptive figures in quarto-hebstr-doc: native `renderings` won (2026-07-20), custom Lua filter dropped; the lightbox does survive `embed-resources`
- [project_autoresearch_skill_deferred.md](project_autoresearch_skill_deferred.md): Skill `uditgoenka/autoresearch` évalué le 2026-09-11 et non adopté; la boucle métrique exige un commit git par itération (interdit ici) et les 13 autres commandes doublonnent ouroboros et les plugins audit/workflow; analyse dans `~/dotfiles/_meta/notes/autoresearch-skill-reco.md`, ne pas la refaire
- [project_arity_evaluation.md](project_arity_evaluation.md): Arity 0.23.0 tested 2026-09-10 against air + jarl and not adopted; the line-break recompaction is pinned by an upstream test fixture and `lint --fix` strips rlang `!!`; measurements in `~/dotfiles/_meta/notes/arity-trial.md`, ne pas les refaire
- [project_edstr_vignettes_pkgdown_articles.md](project_edstr_vignettes_pkgdown_articles.md): edstr vignettes use plain quarto html (pkgdown's own theme), shipped to CRAN again (2026-07-22); the hebstr-doc standalone theme was tried and reverted (renders dirty inside pkgdown)
- [project_md_nesrine_render_pitfalls.md](project_md_nesrine_render_pitfalls.md): md-nesrine and sibling hebstr projects, render and Rscript pitfalls: `filter` resolving to `stats::filter` from `.Rprofile` setup, `as_factor` dropping labels, commented chunks still running, the flextable docx path and its ordering constraints
- [project_edscrib_annotation_package.md](project_edscrib_annotation_package.md): eds-avc's annotation socle moving into `edscrib` (`~/Documents/packages/py-edscrib`): where the tracker lives, the settled constraints, and the two guards `run_app` must carry
- [project_prise_timeline_fixture.md](project_prise_timeline_fixture.md): eds-prise timeline stays queried live from the CHU EDS Oracle base (`annot/lib/eds.py`, python-oracledb thin, tns `vlp`, service account, app on the CHU workstation); replaced the simulated-stay fixture and its parquet snapshot on 2026-08-02
- [project_yaml_gate_deferred.md](project_yaml_gate_deferred.md): YAML gate for dotfiles researched 2026-07-30 then deferred; write-up in `_meta/notes/yaml-gate-reco.md` (prettier over yamlfmt, yamllint blocked by an npm `yaml-lint` symlink, no schema exists for Quarto configs), do not redo the research
- [project_hebstr_add_note_migration.md](project_hebstr_add_note_migration.md): hebstr's add_note() takes gtsummary and must be piped before tbl_format() (2026-07-17); re-verified 2026-07-28 — umb-coco is migrated, quarto-stat was never a call site, one site remains in ipl-sca (SHA-pinned, so it breaks on sync, not silently)
- [project_ordo_typst_template.md](project_ordo_typst_template.md): `~/admin/pro-ordo/ordo.qmd`, Quarto/Typst prescription letterhead mirroring pro-cv, dual blank/filled mode driven by Typst `#let` params (not YAML), strikeout bar as a `1fr` block below the signature and never pre-printed; decisions in `.claude/DESIGN.md` (gitignored), open items are the paper print check and the header contacts
- [project_ue5_horror_game.md](project_ue5_horror_game.md): Solo UE5 horror game project started 2026-08-19 (Darkphobia Games process); research note in `~/dotfiles/_meta/notes/ue5-horror-solo-pipeline.md`, script v1, floor plan and Fab shopping list in `~/Documents/sandbox/ue5-le-relais/`, blocked on Windows-machine access, PLAN.md deliberately deferred

## Reference
- [reference_syncthing_ghost_records.md](reference_syncthing_ghost_records.md): A device stuck at "Syncing 99 %" on paths ignored everywhere: find the stale holder with `syncthing debug database-file` (`G` without `v`), never `availability`; `ju-DG` and `ju-GO` hold hand-copied `.stignore` that need the new patterns pasted in the app after every repo change
- [reference_dbplyr_in_vs_semi_join.md](reference_dbplyr_in_vs_semi_join.md): `filter(id %in% <100k R values>)` against a lazy duckdb table inlines every value into a SQL `IN` list and never returns; keep both sides lazy and `semi_join`, then `collect()` last
- [feedback_verify_gt_output_on_render.md](feedback_verify_gt_output_on_render.md): gtsummary/gt formatting is verified on the rendered PNG, never on `table_styling` or the emitted HTML; a body-rewriting helper can flatten a hierarchy the styling frame still describes correctly, and an empty `by` level ships as `0 (NA)`
- [reference_dbplyr_null_semantics.md](reference_dbplyr_null_semantics.md): dbplyr aggregates follow SQL NULL semantics, not R's: `sum()` skips NULLs instead of propagating, so `sum(if_else(col == v, 1L, 0L))` undercounts a mixed group and returns NULL for an all-NULL one, and `col == v` is never TRUE on a NULL; wrap in `coalesce()` and verify on the written artifact
- [reference_streamlit_altair_charts.md](reference_streamlit_altair_charts.md): Altair charts in Streamlit: height and padding traps, legends ignoring the theme, the single shared tooltip element, and which HTML embedding runs JS
- [reference_streamlit_dataframe_styler.md](reference_streamlit_dataframe_styler.md): st.dataframe keeps only `color`, `background-color` and `font-weight` from a pandas Styler; the grid is a canvas so no CSS reaches rows, and the selection column cannot be restyled
- [reference_editorconfig.md](reference_editorconfig.md): EditorConfig for team/multi-editor formatting consistency
- [reference_r_check_separable_model_tests.md](reference_r_check_separable_model_tests.md): R model-tidying tests (gtsummary/broom) can pass devtools::test() but fail R CMD check when the model is separable; de-separate test data, run check() not just test()
- [reference_claude_code_best_practices.md](reference_claude_code_best_practices.md): Claude Code internals: CLAUDE.md scopes, hook events, auto-memory, MCP rejection rationale, notable settings.json keys (2026-05-02)
- [reference_todo_sync.md](reference_todo_sync.md): `todo-sync` shell command prints aggregated TODO.md items to stdout as a markdown table
- [reference_pkgdown_ignores_rbuildignore.md](reference_pkgdown_ignores_rbuildignore.md): pkgdown ignores .Rbuildignore — every root `*.md` becomes a published page (titled `NA` without an H1) and every vignette an article; only the file's location excludes it
- [reference_pkgdown_assets_extra_css.md](reference_pkgdown_assets_extra_css.md): only `pkgdown/assets/` is copied to the site root, and a relative `url()` works in `extra.css` (copied) but never in `extra.scss` (compiled into `deps/`); a Google font goes through `template.bslib` instead, and the whole failure is silent on both ends
- [reference_claude_code_marketplace_mechanics.md](reference_claude_code_marketplace_mechanics.md): Marketplace + plugin lifecycle; cache vs live edits for directory-source plugins; the two files holding a directory source path and the refresh failure a moved repo causes
- [reference_modern_r_model_tooling.md](reference_modern_r_model_tooling.md): Current-practice R packages for model eval/comparison/effect interpretation (easystats, broom, yardstick, marginaleffects); tool-selection reference, surface on user request, not spontaneously on analysis code
- [reference_nanoparquet_col_select.md](reference_nanoparquet_col_select.md): nanoparquet 0.5.1 `col_select` silently drops NAs and shifts values up within a row group; NA-free columns are unaffected, which is what hides it. User rule: never use `col_select`, read then `dplyr::select()`. Budget the projection cost when converting
- [reference_regex_engine_accented_text.md](reference_regex_engine_accented_text.md): Big regex alternations over accented text: ICU backtracking vs re2 automaton (615x), RE2 \b is ASCII-only (misses méningé), fold with Latin-ASCII (NOT the 1:1 NFD fold, which drops ligatures) + re2_locate_all + two offset maps ~100x; shipped in edstr
- [reference_technical_doc_pattern.md](reference_technical_doc_pattern.md): Per-script technical doc pattern (hebstr-doc, `{{< filetree >}}` + `{{< script >}}`): comments leave the code and become titled callouts; carries the fixed section plan, the two callout natures, the level-3 heading title, the CSS block and the specificity trap
- [reference_tool_update_routines.md](reference_tool_update_routines.md): devtools-update (cargo-dist installers → /usr/local/bin) vs cargo install-update (crates.io → ~/.cargo/bin, and it needs GGSQL_SKIP_GENERATE=1) vs uv tool upgrade --all (packages → ~/.local/bin) vs uv python upgrade (the interpreter underneath); match routine to install method, don't conflate
- [reference_uv_pip_virtual_env.md](reference_uv_pip_virtual_env.md): `uv pip install` targets $VIRTUAL_ENV over the cwd project, so crossing between two uv projects installs into the wrong venv silently; `env -u VIRTUAL_ENV` is the fix
- [reference_positron_extension_registries.md](reference_positron_extension_registries.md): Positron profile vs global extensions.json; profile is authoritative, never delete folders judged orphan by global alone; a hand-dropped folder never loads under a custom profile and bare --list-extensions gives a false positive; jq-based recovery when registries point to missing folders
- [reference_hebstr_easy_out_subdir.md](reference_hebstr_easy_out_subdir.md): hebstr sha 97e8829 breaks `easy_out()` — per-output kebab-case subfolder (`output/tbl-pop/tbl-pop.png`), `easy_out_map()` removed and folded in, `filename` required for an unnamed object, htmlwidget/flextable now accepted; the flat-path break is silent and a project catches up by deleting its old output contents
- [reference_hebstr_gt_table_width.md](reference_hebstr_gt_table_width.md): hebstr's `tbl_format`/`theme_gt` width is a hard px `table.width` that the gt container can only scroll; the `table.gt_table { max-width: 100% }` theme cap, why `pct()` and `NULL` are both refused (`easy_out()` reads the px back and falls to 700), and how to measure a table's natural width
- [reference_gt_reactable_css_tokens.md](reference_gt_reactable_css_tokens.md): gt validates every colour option and rejects `var()`/`currentColor`/`inherit` (accepts hex, CSS3 names, `transparent`) so only an `!important` override reaches its id-scoped rules, while reactable passes theme strings through untouched; plus the 5-value colour surface, where a dark-theme rules block goes under Quarto, and `as_raw_html()` dying on juicyjuice/V8
- [reference_hebstr_outdec_locale_flag.md](reference_hebstr_outdec_locale_flag.md): hebstr uses global options(OutDec) as its EN/FR locale flag; leaks the comma into third-party numeric round-trips (tidycmprsk::cuminc breaks). Force OutDec="." around the fit; do not refactor hebstr (decided 2026-07-10)
- [reference_hebstr_theme_bar_facets.md](reference_hebstr_theme_bar_facets.md): hebstr `theme_bar(grid = FALSE)` blanks `strip.text` and collides on it if passed through `...` (re-arm as a separate `+ theme()` layer); the 3-color palette needs a darkened `cold[2]` anchor before a 5-category ramp separates
- [reference_quarto_lua_shortcodes.md](reference_quarto_lua_shortcodes.md): Quarto Lua shortcodes and filters: silent traps in kwargs, YAML metadata and named pipes, why format decisions belong in an `is_format` guard, and verified Pandoc filesystem and metadata primitives
- [reference_quarto_meta_shortcode_typst.md](reference_quarto_meta_shortcode_typst.md): `{{< meta a.b >}}` IS substituted inside a ```{=typst} block (scalar nested keys), but sequences and array indexing are not, and a missing key prints `?meta:<key>` into the PDF with only a warning (exit 0); hence declare parameters as Typst `#let` bindings, not in YAML front matter
- [reference_word_embedded_fonts_blocked.md](reference_word_embedded_fonts_blocked.md): The Word installs receiving the user's deliverables refuse OOXML-embedded fonts even though the file conforms (fsType 0, ECMA-376 obfuscation, `embedTrueTypeFonts`, LibreOffice uses them) and Protected View is not the cause; `w:altName` is honoured on the same machine, so design Word outputs around the fallback list or install the font on the reading machines
- [reference_quarto_extension_format_knitr.md](reference_quarto_extension_format_knitr.md): Quarto extension knitr options per format, svglite and systemfonts font registration traps, and what an extension can ship but not execute
- [reference_quarto_custom_format_render_target.md](reference_quarto_custom_format_render_target.md): A custom format resolves to its base before knitr starts, so `knitr::pandoc_to()`/`is_html_output()` discriminate `hebstr-doc-docx` from `hebstr-doc-html` in the setup chunk, and `--to <custom-format>` renders a document that does not declare it: one `.qmd` serves an HTML and a Word deliverable without duplication or `content-visible` blocks
- [reference_citeproc_bib_vs_csl.md](reference_citeproc_bib_vs_csl.md): citeproc title casing differs between `.bib` and CSL-JSON/YAML bibliographies, how to protect French titles, and the conversion and CSL-YAML traps
- [reference_quarto_extension_install_resolution.md](reference_quarto_extension_install_resolution.md): `quarto add owner/repo` (and `@latest`) downloads the main-branch tarball, never the last release; only `@<tag>` pins, and Quarto never queries the GitHub releases API for an extension, so the tag is the whole distribution mechanism and the Release is human-facing
- [reference_quarto_file_outside_render_list.md](reference_quarto_file_outside_render_list.md): A `.qmd` outside the project `render:` list gets no `_quarto.yml` metadata (`lang` fell back to the theme's `en`) and no `output-dir`, but pre-render and the directory `_metadata.yml` still apply
- [reference_quarto_doc_crossref_overrides_format.md](reference_quarto_doc_crossref_overrides_format.md): A top-level `crossref:` in a document dropped the extension format's `title-delim` ("Tableau 1:") although `quarto inspect` showed it merged; nest it under the format and check the rendered caption
- [reference_pandoc_smart_abbreviations_nbsp.md](reference_pandoc_smart_abbreviations_nbsp.md): Pandoc `smart` turns the space after "et al." and other abbreviations into U+00A0; an empty `abbreviations:` file disables it; `pandoc -t markdown` also writes a LineBreak ending a Strong as `**x:\**`, which reads back broken
- [reference_quarto_r_env_var.md](reference_quarto_r_env_var.md): `QUARTO_R` is exported in the profile and beats `PATH`, so an `rv` project pinned to another `r_version` silently drops into safe mode and the render dies on "knitr is not available" rather than on the mismatch; override it per command, `rig default` does not help
- [reference_lua_gate_quarto_filters.md](reference_lua_gate_quarto_filters.md): Lua gate for Quarto filters in practice: what a project must commit, why the check targets the workspace root, and how filters are tested
- [reference_bash_tool_pitfalls.md](reference_bash_tool_pitfalls.md): Bash tool shell traps: `find` is bfs (a path starting with `-` becomes an option, use `/usr/bin/find`), `pkill -f`/`pgrep -f` match the tool's own command line (exit 144, false "Positron running"), `rg -h` is help, sudo cannot prompt and the `!` prefix has no TTY (hand sudo commands to the user's terminal), bundled `rm -rf` gets declined as a whole
- [reference_uv_cache_internals.md](reference_uv_cache_internals.md): `uv cache prune` keeps unlinked archive entries; live entries mix hardlinked and copied files so only whole-entry deletion is safe; uvx/uv run MCP servers hold the cache lock all session (lock-gated cleanup never runs, use an age floor); deleting an entry costs a download; prek and Anki embed their own uv caches; rv links whole packages
- [reference_pak_metadata_cache.md](reference_pak_metadata_cache.md): pak keeps one `pkgs-<hash>.rds` per configuration in `_metadata` forever (`cache_clean()` ignores it); `meta_summary()[["current_db"]]` names the live one; `meta_clean()` prompts unless `force = TRUE`; `R_PKG_CACHE_DIR` redirects; rebuild from empty 94 s

## Feedback
- [feedback_gtsummary_stacked_blocks.md](feedback_gtsummary_stacked_blocks.md): stacked gtsummary blocks open with an ordinary label row per block and indented rows under it, via `add_variable_group_header()` before `tbl_stack()`; `tbl_stack(group_header=)` and `tbl_strata(.header=)` render full-width gt group rows outside the striping, rejected by the user (2026-09-02)
- [feedback_probe_must_test_the_deciding_form.md](feedback_probe_must_test_the_deciding_form.md): a measurement used to rule an option out must have exercised that option's strongest form; a probe over the weakest form yields a true observation and a false conclusion, and nothing downstream ever revisits a closed option
- [feedback_commit_sequence_add_all.md](feedback_commit_sequence_add_all.md): Commit proposals: `git add .` when the commit takes everything `git status` shows and nothing earlier in the sequence moves the ignore state, else paths or `git add -u`, never `git add -A`; paths only from `git status`; format lives in the `commit` skill since 2026-09-22
- [feedback_r_environment.md](feedback_r_environment.md): R environment idioms (rv not renv, `system2` over `system`, rv sync auto-runs at R startup via .Rprofile)
- [feedback_env_var_presence_check.md](feedback_env_var_presence_check.md): `${VAR:-fallback}` prints the secret when the variable IS set; test presence with `[ -n "$VAR" ]` or `${VAR:+set}` alone
- [feedback_benchmark_power_state.md](feedback_benchmark_power_state.md): Benchmarks on this laptop carry a power state: ~2x slower on battery than on AC, uniformly; ~8% run-to-run noise inside a regime; claim gains as shares from paired runs, never as wall-clock deltas
- [feedback_verify_before_claiming.md](feedback_verify_before_claiming.md): 6 hard rules for verifying factual claims; detail behind the CLAUDE.md rule "never state a verifiable fact without checking it first"
- [feedback_check_before_hand_rolling.md](feedback_check_before_hand_rolling.md): query the installed library before hand-rolling a generic helper, or before pricing a route as "we would have to build X", then escalate to a ranked web index when disk comes up empty; the directional bias of recall toward stale idioms, why no linter catches the duplicate, and why a hit outside the dependencies is a proposal rather than an action
- [feedback_load_matching_skill.md](feedback_load_matching_skill.md): Load a matching installed skill before producing the artifact; mirroring a neighbouring file's conventions reproduces its blind spots (bats tests that asserted removals but not the reported status, which was the bug)
- [feedback_reviewer_review_testing_scope.md](feedback_reviewer_review_testing_scope.md): review-testing scopes to test files changed on the branch; pass an explicit scope override to review an existing suite on a clean tree
- [feedback_review_severity_personal.md](feedback_review_severity_personal.md): Calibrate review severity down for personal/internal packages
- [feedback_review_severity_md_nesrine.md](feedback_review_severity_md_nesrine.md): Calibrate reviews of the md-nesrine render journal: no journal line for a failed render (errors reach the redirected render output, failure lines would log half-rewritten SVG hashes), no commit-identification field (`git blame` on the tracked jsonl already maps a line to its commit)
- [feedback_review_severity_stat_reports_fr.md](feedback_review_severity_stat_reports_fr.md): French statistical reports: don't flag established untranslated English terms, execute the code before filing a claim about a method or about hebstr's behaviour (the parametric split is declarative, an absent DM column means zero missing), and don't file prose that would only restate a caption or a table
- [feedback_review_severity_shell_installers.md](feedback_review_severity_shell_installers.md): Calibrate review severity for personal shell installers (quarto-update, positron-update, etc.) and Claude Code hook scripts
- [feedback_review_severity_hebstr_doc.md](feedback_review_severity_hebstr_doc.md): Calibrate review of the quarto-hebstr-doc Quarto extension (filetree.lua): dismissed idiom/perf/duplication findings (duplicated kw helper, hardcoded / separator, no list_directory cache, warn-vs-is_format guard asymmetry, terse names, depth=0 clamp)
- [feedback_review_severity_bats_tests.md](feedback_review_severity_bats_tests.md): Calibrate review severity for bats `.bats` test files covering personal shell tooling
- [feedback_review_severity_bash_dotfiles.md](feedback_review_severity_bash_dotfiles.md): Calibrate reviews of the `bash/` stow package: SC1090 directives on `~` paths are required, the firefox alias is single-machine by design, `bind` warnings not reproduced on bash 5.2
- [feedback_review_severity_claude_rules.md](feedback_review_severity_claude_rules.md): Calibrate reviews of `.claude/rules/` files: recurring false positives to skip
- [feedback_review_severity_profiles_scaffolds.md](feedback_review_severity_profiles_scaffolds.md): Calibrate audits of the `_meta/profiles/` config templates: skip repo-specific-exclude-as-footgun, fewer-languages-than-sibling-scaffold, duplicate-a-value-a-formatter-config-owns (3 recurring false positives)
- [feedback_review_severity_claude_config.md](feedback_review_severity_claude_config.md): Calibrate audits of `~/.claude` harness config (settings.json, hooks): skip PWD-guard expansion, rg-in-hook-scripts, message-aware notifications, WebFetch allow-list expansion, race-condition hardening on best-effort cleanup hooks (5 recurring false positives)
- [feedback_review_severity_claude_md_content.md](feedback_review_severity_claude_md_content.md): Calibrate content audits of CLAUDE.md instructions: skip under-defined-term-with-anchored-criteria, opt-out-against-user-preference, trigger-owned-upstream, deliberate-bias-catching-cost, harness-backstopped-efficiency-hint, length-offload-as-walking-fix, already-closed-by-existing-wording, mechanism-misread, self-denied-misreading, guard-declared-redundant-without-following-the-pointer, and in a refactor safety-net-extraction and hook-over-prose (12 recurring false positives)
- [feedback_review_severity_skill_audits.md](feedback_review_severity_skill_audits.md): Calibrate reviews of SKILL.md files and agent definitions: recurring false positives to skip (explicit-invocation gate and workflow:sync specifics included), and reading a SKILL.md as a spec
- [feedback_review_severity_hebstr.md](feedback_review_severity_hebstr.md): Calibrate reviews of the hebstr R package: deliberate designs not to flag
- [feedback_review_severity_edstr.md](feedback_review_severity_edstr.md): Calibrate reviews of the edstr R package: deliberate designs not to flag
- [feedback_review_severity_eds_prise.md](feedback_review_severity_eds_prise.md): Calibrate reviews of eds-prise (R pipeline and Streamlit annotation app): designs not to flag, measurement traps, and the remedy-level false positives of each pass
- [feedback_review_severity_edscrib.md](feedback_review_severity_edscrib.md): Calibrate reviews of the edscrib annotation socle (~/Documents/packages/py-edscrib): the write path not to soften, and what each pass taught about remedies, guards and measurement
- [feedback_review_severity_litrev_mcp.md](feedback_review_severity_litrev_mcp.md): Calibrate adversarial audits on litrev-mcp (FastMCP server): in-memory pipeline ≠ file mutation, reactive env-tips are idiomatic FastMCP, empty-string is the schema sentinel, MCP error dicts are LLM-consumed (no error-code taxonomy), don't tighten regex on user-authored fields (5 recurring false positives, blindspot 2026-05-17)
- [feedback_review_severity_eds_avc.md](feedback_review_severity_eds_avc.md): Calibrate reviews of eds-avc's `pred/` orchestrator and its R log helpers
- [feedback_review_severity_heuristic_code.md](feedback_review_severity_heuristic_code.md): Stop rule for adversarial reviews of heuristic code (free-text parsers, discovery scans): accept only findings reproduced on the real corpus, contradicting official docs, or crashing; hypothetical edge cases are NOTED (three non-converging rounds on scan-reviewers.py, stop called 2026-09-15)
- [feedback_litrev_pipeline_patterns.md](feedback_litrev_pipeline_patterns.md): Recurring `/litrev` scoping decisions: post-hoc restrict above 150 incl., skip backward snowball with recent window, audit_claims/synthesize DOI conflict, bypass validate_gate stall, strip pseudo-GRADE, two-pass post-gen prose cleanup (6 rules, epidemio PRISE 2026-05-17/18)
- [feedback_verify_quarto_theming.md](feedback_verify_quarto_theming.md): For any SCSS/CSS/theme edit in a Quarto project or extension, render and inspect the compiled CSS (decode the `data:text/css` links), and measure computed styles with headless chromium rather than iterating on screenshots
- [feedback_quarto_render_disabled_chapter.md](feedback_quarto_render_disabled_chapter.md): Rendering a `.qmd` commented out of a book's `chapters:` can make Quarto uncomment it in `_quarto.yml`; check `git status` on the project config before calling the render gate passed
- [feedback_french_prose.md](feedback_french_prose.md): FR prose: authorities, anglicism policy, typography, register: full reference for prose work
- [feedback_review_third_party_content.md](feedback_review_third_party_content.md): Don't apply CLAUDE.md prose style rules to third-party skill descriptions, plugin docs, or forked content
- [feedback_external_pattern_imports.md](feedback_external_pattern_imports.md): Filter for external Claude Code config imports: identify the consumer that justifies the format before importing (4 false positives in solatis audit 2026-05-16)
- [feedback_verify_after_install.md](feedback_verify_after_install.md): Smoke-test new tools/hooks/integrations end-to-end before marking done; never suppress stderr on fresh config
- [feedback_shell_grep_pipefail.md](feedback_shell_grep_pipefail.md): shell shapes that fail or lie under `set -Eeuo pipefail` (grep mid-pipeline, process substitution, `&&` lists, `jq -e`/`-r`), with fixes and an audit grep
- [feedback_rg_misses_gitignored_trackers.md](feedback_rg_misses_gitignored_trackers.md): bare `rg` skips hidden and gitignored paths, so a dependency grep reports the `.claude/` trackers as clean; use `--hidden --no-ignore` when asking what references a symbol
- [feedback_git_clean_tree_hides_ignored.md](feedback_git_clean_tree_hides_ignored.md): `git status --porcelain` omits ignored files, so a clean-tree guard passes on a working tree holding an ignored-at-tip file that a replayed commit adds back and the rebase aborts on; check `--ignored` against the replayed commits' file list first (measured on quarto-hebstr-slide, README.md)
- [feedback_line_number_cross_refs.md](feedback_line_number_cross_refs.md): incidents and detail behind the cite-by-name rule of CLAUDE.md, including when a drifted reference is left as record
- [feedback_prose-lint-write.md](feedback_prose-lint-write.md): prose hygiene applies to user-facing prose only (which trees are exempt, why the stowed global config is not), soft-wraps belong to panache, typographic vs rhetorical em/en dash distinction, when to invoke /workflow:write
- [feedback_no_bulk_regex_for_contextual_skills.md](feedback_no_bulk_regex_for_contextual_skills.md): Don't substitute a contextual editing skill (workflow:write, audit reviewers) with a bulk regex when per-case Edit is blocked; surface the blocker instead
- [feedback_audit_walkthrough.md](feedback_audit_walkthrough.md): Running `/audit:walkthrough` and `/audit:blindspot`: triage format, the calibration loader, how reliable each blindspot bucket measured, L1/L2 briefing, and the judge's size cap
- [feedback_format_hook_strips_new_import.md](feedback_format_hook_strips_new_import.md): the format-on-edit hook runs `ruff check --fix`, so a Python import added before its first use is stripped as F401 between two Edits; write the usage first
- [feedback_browser_layout_probe.md](feedback_browser_layout_probe.md): Inspect a rendered layout yourself with headless chromium (screenshot, DOM dump, CDP for hover and computed styles) instead of asking the user; what fails on Streamlit pages
- [feedback_personal_working_files.md](feedback_personal_working_files.md): rationale and edge cases behind the CLAUDE.md write-gate on NOTES.md / TODO.md / CALENDRIER.md (reading is free, writing never is, and what the read still does not license)
- [feedback_ouroboros_tools.md](feedback_ouroboros_tools.md): Ouroboros evaluate and qa: which honours consensus, why that consensus is Claude alone under the `claude_code` backend, and the race with a fix landing mid-evaluation
- [feedback_xdg_open.md](feedback_xdg_open.md): xdg-open silently fails on file:// in Positron/VSCode; use `python3 -m http.server` for HTML preview
- [feedback_verify_native_gc_semantics.md](feedback_verify_native_gc_semantics.md): Never call a cache "covered" from a GC command's name: read its help and test on a scratch copy; uv cache prune, prek cache gc, pak::cache_clean and flatpak uninstall --unused each left hundreds of MiB to GiB behind (2026-09-13)
- [feedback_session_tool_residue.md](feedback_session_tool_residue.md): A tool Claude launches and a timeout kills skips its cleanup: give it a mktemp state dir removed by an EXIT trap in the same call; killed headless chromium runs leaked 26.8 GiB of profiles in under a month
- [feedback_edit_tool_unicode_escape.md](feedback_edit_tool_unicode_escape.md): Tool parameters decode a four-hex-digit `\u` escape into the literal character (U+00A0 landed invisibly in R source, 2026-09-16, reproduced with Edit and Write); in R write `\u{a0}`, check with `rg -c $'\xc2\xa0'`
