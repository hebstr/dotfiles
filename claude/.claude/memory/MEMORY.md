# Memory Index

## User
- [user_profile.md](user_profile.md): Environment, accounts, and context not in CLAUDE.md
- [user_quarto_typst_only.md](user_quarto_typst_only.md): Quarto PDF via Typst only; user does not use LaTeX (do not suggest the `format: pdf` LaTeX path)

## Project
- [project_qmd_format_hook.md](project_qmd_format_hook.md): Quarto/.qmd formatting via panache (Rust formatter, wrap=semantic), config stowed, plus the `panache-format` prek commit gate; a project `panache.toml` replaces the global wholesale, `extend-exclude` vs `exclude` gotchas, the CLI/LSP split on excludes that governs Positron's format-on-save, and panache rewriting tabs inside code fences
- [project_review_workflow_backlog.md](project_review_workflow_backlog.md): Implementation backlog from the 2026-03 code review glowup audit (hebstr tests, CI, data validation)
- [project_rv_install_deployment.md](project_rv_install_deployment.md): rv-install and quarto-update are stored in dotfiles but deployed on multi-user servers; affects threat model and portability (no amd64 assumption)
- [project_litrev_separate_from_hebstr.md](project_litrev_separate_from_hebstr.md): Decision (2026-04-26): keep litrev separate from hebstr marketplace; do not re-propose merge
- [project_hebstr_doc_adaptive_figures.md](project_hebstr_doc_adaptive_figures.md): Theme-adaptive figures in quarto-hebstr-doc: native `renderings` won (2026-07-20), custom Lua filter dropped; the lightbox does survive `embed-resources`
- [project_edstr_vignettes_pkgdown_articles.md](project_edstr_vignettes_pkgdown_articles.md): edstr vignettes use plain quarto html (pkgdown's own theme), shipped to CRAN again (2026-07-22); the hebstr-doc standalone theme was tried and reverted (renders dirty inside pkgdown)
- [project_md_nesrine_render_pitfalls.md](project_md_nesrine_render_pitfalls.md): md-nesrine blockers (Quarto render AND any Rscript/source entry point): conflicts_prefer lost from .Rprofile startup, bare filter resolves to stats::filter → "object '<col>' not found" (a `setHook(packageEvent("stats", "attach"), ...)` in `.Rprofile` fixes it structurally, measured 2026-08-10 but reverted by the user, eds-prise living on the re-assert atop `scripts/_setup.R`; `.First` does NOT work, it runs before `.First.sys()` attaches the defaults; fallbacks stay re-assert before sourcing, or `library(tidyverse)` atop the script; hits any hebstr-stack project); as_factor strips var_label on unlabelled int vars (use labelled::to_factor); HTML-commenting an {r} chunk doesn't disable it (use #| eval: false, keep --> off the fence line); docx now renders via flextable under options(hebstr.docx = TRUE) (theme_gt.docx gone, justify error resolved), which forces add_note() before tbl_format() and makes easy_out() skip the export, so post-processing of an easy_out artifact must be idempotent
- [project_edscrib_annotation_package.md](project_edscrib_annotation_package.md): The eds-avc Streamlit annotation socle is moving into the `edscrib` package (`~/Documents/packages/py-edscrib`); tracker lives in the gitignored `eds-avc/.claude/ANNOT-PKG.md`, plus the constraints not to re-litigate (no path dependency, PyPI-only deps, nothing AVC-specific descends) and the two guards `run_app` must carry (gated locked output write, ordered `id_doc` identity check)
- [project_prise_timeline_fixture.md](project_prise_timeline_fixture.md): eds-prise timeline stays queried live from the CHU EDS Oracle base (`annot/lib/eds.py`, python-oracledb thin, tns `vlp`, service account, app on the CHU workstation); replaced the simulated-stay fixture and its parquet snapshot on 2026-08-02
- [project_yaml_gate_deferred.md](project_yaml_gate_deferred.md): YAML gate for dotfiles researched 2026-07-30 then deferred; write-up in `_meta/notes/yaml-gate-reco.md` (prettier over yamlfmt, yamllint blocked by an npm `yaml-lint` symlink, no schema exists for Quarto configs), do not redo the research
- [project_hebstr_add_note_migration.md](project_hebstr_add_note_migration.md): hebstr's add_note() takes gtsummary and must be piped before tbl_format() (2026-07-17); re-verified 2026-07-28 — umb-coco is migrated, quarto-stat was never a call site, one site remains in ipl-sca (SHA-pinned, so it breaks on sync, not silently)

## Reference
- [reference_streamlit_altair_charts.md](reference_streamlit_altair_charts.md): Altair in Streamlit: `properties(height=)` is the TOTAL svg height under `autosize: fit` (a legend can starve the plot area to 0 px and push the axis out of the viewBox), a scalar Vega `padding` crashes the component, scale padding vs view padding, the tooltip is one shared `#vg-tooltip-element` on `document.body` with no per-datum hook, `st.html(unsafe_allow_javascript=True)` executes JS and `components.v1.html` is deprecated for `st.iframe`
- [reference_editorconfig.md](reference_editorconfig.md): EditorConfig for team/multi-editor formatting consistency
- [reference_r_check_separable_model_tests.md](reference_r_check_separable_model_tests.md): R model-tidying tests (gtsummary/broom) can pass devtools::test() but fail R CMD check when the model is separable; de-separate test data, run check() not just test()
- [reference_claude_code_best_practices.md](reference_claude_code_best_practices.md): Claude Code internals: CLAUDE.md scopes, hook events, auto-memory, MCP rejection rationale, notable settings.json keys (2026-05-02)
- [reference_todo_sync.md](reference_todo_sync.md): `todo-sync` shell command prints aggregated TODO.md items to stdout as a markdown table
- [reference_pkgdown_ignores_rbuildignore.md](reference_pkgdown_ignores_rbuildignore.md): pkgdown ignores .Rbuildignore — every root `*.md` becomes a published page (titled `NA` without an H1) and every vignette an article; only the file's location excludes it
- [reference_claude_code_marketplace_mechanics.md](reference_claude_code_marketplace_mechanics.md): Marketplace + plugin lifecycle; cache vs live edits for directory-source plugins
- [reference_modern_r_model_tooling.md](reference_modern_r_model_tooling.md): Current-practice R packages for model eval/comparison/effect interpretation (easystats, broom, yardstick, marginaleffects); tool-selection reference, surface on user request, not spontaneously on analysis code
- [reference_nanoparquet_col_select.md](reference_nanoparquet_col_select.md): nanoparquet 0.5.1 `col_select` silently drops NAs and shifts values up within a row group; NA-free columns are unaffected, which is what hides it. User rule: never use `col_select`, read then `dplyr::select()`. Budget the projection cost when converting
- [reference_regex_engine_accented_text.md](reference_regex_engine_accented_text.md): Big regex alternations over accented text: ICU backtracking vs re2 automaton (615x), RE2 \b is ASCII-only (misses méningé), fold with Latin-ASCII (NOT the 1:1 NFD fold, which drops ligatures) + re2_locate_all + two offset maps ~100x; shipped in edstr
- [reference_tool_update_routines.md](reference_tool_update_routines.md): devtools-update (cargo-dist installers → /usr/local/bin) vs cargo install-update (crates.io → ~/.cargo/bin) vs uv tool upgrade --all (→ ~/.local/bin); match routine to install method, don't conflate
- [reference_uv_pip_virtual_env.md](reference_uv_pip_virtual_env.md): `uv pip install` targets $VIRTUAL_ENV over the cwd project, so crossing between two uv projects installs into the wrong venv silently; `env -u VIRTUAL_ENV` is the fix
- [reference_positron_extension_registries.md](reference_positron_extension_registries.md): Positron profile vs global extensions.json; profile is authoritative, never delete folders judged orphan by global alone; jq-based recovery when registries point to missing folders
- [reference_hebstr_outdec_locale_flag.md](reference_hebstr_outdec_locale_flag.md): hebstr uses global options(OutDec) as its EN/FR locale flag; leaks the comma into third-party numeric round-trips (tidycmprsk::cuminc breaks). Force OutDec="." around the fit; do not refactor hebstr (decided 2026-07-10)
- [reference_hebstr_theme_bar_facets.md](reference_hebstr_theme_bar_facets.md): hebstr `theme_bar(grid = FALSE)` blanks `strip.text` and collides on it if passed through `...` (re-arm as a separate `+ theme()` layer); the 3-color palette needs a darkened `cold[2]` anchor before a 5-category ramp separates
- [reference_quarto_lua_shortcodes.md](reference_quarto_lua_shortcodes.md): Quarto Lua shortcodes: absent kwargs arrive as empty Inlines (truthy) and silently kill fallback logic; Pandoc parses YAML metadata scalars as inline Markdown and silently mutilates Lua patterns (`__pycache__` becomes `pycache`); YAML 1.1 hands unquoted `no`/`yes` to Lua as booleans that crash `pandoc.Plain` (and `%YAML 1.2` cannot switch it off); `io.open` on a named pipe hangs the render forever; plus verified Pandoc filesystem/metadata primitives (file kind read off the `list_directory` error, sidecar YAML via pandoc.read, MetaList vs MetaInlines)
- [reference_quarto_extension_format_knitr.md](reference_quarto_extension_format_knitr.md): A Quarto extension can scope `knitr: opts_chunk:` to one format instead of `common:` (verified), which is how hebstr-doc pins `dev: svglite` to HTML; `!expr` does NOT evaluate in `_extension.yml` (use a plain YAML map); svglite writes the family it matched not the one asked for (silent fallback defeats the webfont), `system_fonts`/`user_fonts` are both Superseded, `dev.args` is replaced wholesale by a chunk; an extension cannot execute R but can ship it (`fonts/register.R`, self-locating, sourced by the project); `register_font()` errors on an installed family so its guard is mandatory; and woff/woff2 are unreadable by Typst `font-paths` and `systemfonts::add_fonts()` alike
- [reference_lua_gate_quarto_filters.md](reference_lua_gate_quarto_filters.md): Lua gate (rules/lua.md, stylua + lua-language-server --check): the check targets the workspace ROOT not a file; per-project needs a committed no-Generator .luarc.json + vendored .luals stubs + stylua.toml; filters are shortcodes tested by direct handler invocation (not run_lua_filter) via `quarto pandoc lua tests/run.lua`; undefined-field fires only on @class types; the gate does not overlap adversarial-audit findings (0/29 measured), its value is forward-looking

## Feedback
- [feedback_commit_sequence_add_all.md](feedback_commit_sequence_add_all.md): Never propose `git add -A` in a multi-commit sequence; use `git add -u` or explicit paths so staging cannot depend on an earlier command having run
- [feedback_r_environment.md](feedback_r_environment.md): R environment idioms (rv not renv, `system2` over `system`, rv sync auto-runs at R startup via .Rprofile)
- [feedback_benchmark_power_state.md](feedback_benchmark_power_state.md): Benchmarks on this laptop carry a power state: ~2x slower on battery than on AC, uniformly; ~8% run-to-run noise inside a regime; claim gains as shares from paired runs, never as wall-clock deltas
- [feedback_verify_before_claiming.md](feedback_verify_before_claiming.md): 6 hard rules for verifying factual claims; detail behind the CLAUDE.md rule "never state a verifiable fact without checking it first"
- [feedback_load_matching_skill.md](feedback_load_matching_skill.md): Load a matching installed skill before producing the artifact; mirroring a neighbouring file's conventions reproduces its blind spots (bats tests that asserted removals but not the reported status, which was the bug)
- [feedback_review_workflow.md](feedback_review_workflow.md): Preferred review workflow: 3 background specialists + 2-3 foreground agents by facet, consolidated report, then `/audit:walkthrough`
- [feedback_reviewer_review_testing_scope.md](feedback_reviewer_review_testing_scope.md): review-testing scopes to test files changed on the branch; pass an explicit scope override to review an existing suite on a clean tree
- [feedback_review_severity_personal.md](feedback_review_severity_personal.md): Calibrate review severity down for personal/internal packages
- [feedback_review_severity_shell_installers.md](feedback_review_severity_shell_installers.md): Calibrate review severity for personal shell installers (quarto-update, positron-update, etc.)
- [feedback_review_severity_hebstr_doc.md](feedback_review_severity_hebstr_doc.md): Calibrate review of the quarto-hebstr-doc Quarto extension (filetree.lua): dismissed idiom/perf/duplication findings (duplicated kw helper, hardcoded / separator, no list_directory cache, warn-vs-is_format guard asymmetry, terse names, depth=0 clamp)
- [feedback_review_severity_bats_tests.md](feedback_review_severity_bats_tests.md): Calibrate review severity for bats `.bats` test files covering personal shell tooling
- [feedback_review_severity_claude_rules.md](feedback_review_severity_claude_rules.md): Calibrate review of `.claude/rules/` files: skip duplication-from-CLAUDE.md, dedup-of-boilerplate, version-trace headers, review-calibration-as-writing-rule, alternate-tool-removal-due-to-overlap, prek-bash-exclusion-as-conflict, operationalized-rule-flagged-as-ambiguous, paths-glob-widening-for-a-mechanically-enforced-gate, ruff-missing-from-one-tool-one-binary, house-wide-convention-as-per-file-defect, portability-hedging-for-absent-platforms, hasattr-over-from-import, and-chain-in-r-shell-as-python-defect (13 recurring false positives)
- [feedback_review_severity_profiles_scaffolds.md](feedback_review_severity_profiles_scaffolds.md): Calibrate audits of the `_meta/profiles/` config templates: skip repo-specific-exclude-as-footgun, fewer-languages-than-sibling-scaffold, duplicate-a-value-a-formatter-config-owns (3 recurring false positives)
- [feedback_review_severity_claude_config.md](feedback_review_severity_claude_config.md): Calibrate `/health` and audits of `~/.claude` harness config (settings.json, hooks): skip PWD-guard expansion, rg-in-hook-scripts, message-aware notifications, WebFetch allow-list expansion, race-condition hardening on best-effort cleanup hooks (5 recurring false positives)
- [feedback_review_severity_claude_md_content.md](feedback_review_severity_claude_md_content.md): Calibrate content audits of CLAUDE.md instructions: skip under-defined-term-with-anchored-criteria, opt-out-against-user-preference, trigger-owned-upstream, deliberate-bias-catching-cost, harness-backstopped-efficiency-hint, length-offload-as-walking-fix, already-closed-by-existing-wording, mechanism-misread (8 recurring false positives)
- [feedback_claude_md_refactor.md](feedback_claude_md_refactor.md): CLAUDE.md Progressive Disclosure refactor: do not extract always-on safety-net checklists (silent-failure), do not automate via hook what prose pointers already cover (2 recurring false positives)
- [feedback_review_severity_skill_audits.md](feedback_review_severity_skill_audits.md): Calibrate skill-adversary/blindspot/sweep on SKILL.md files: skip harness portability, declared-prior anchoring, CLAUDE.md duplication, explicit-invocation FN concerns (4 recurring false positives)
- [feedback_review_severity_hebstr.md](feedback_review_severity_hebstr.md): Calibrate reviews of the hebstr R package: theme_gt is deliberately strict (requires set_opts, don't flag its clean abort as divergence); .name-parametrized opts is a latent capability, don't flag hardcoded "opts" in .text_font/easy_view; get_xlsx(color=) tolerating a name that matches no column is the shared-palette design, don't suggest warning on it; testthat edition 3 pins OutDec so no test depends on the ambient locale and the OutDec="." guards stay; the PinnedAlpha opts stub cannot be rebuilt via set_opts; svg_crop's literal match on svglite's background rect is test-guarded, don't loosen it; easy_out's "Object:" banner reports the object so the pre-suffix name is right there; tbl_format()'s coef-path and non-gtsummary-guard tests live in test-gtsum_format.R, not test-tbl_format.R
- [feedback_review_severity_edstr.md](feedback_review_severity_edstr.md): Calibrate reviews of the edstr R package: easy_format's `">`-anchored HTML regex is correct, `token` vs `ngrams` are different concepts (don't unify), gitignored working files (dev/TODO.md, cran-comments.md) are out of scope, CI provisions Quarto/Pandoc transitively via setup-r-dependencies, `.extract_parse_concepts` stays permissive by design (the refusal lives in edstr_extract), five reserved output column names
- [feedback_review_severity_eds_prise.md](feedback_review_severity_eds_prise.md): Calibrate reviews of eds-prise, both subsystems. R analysis pipeline (scripts/, hebstr stack): stopifnot blocks are tripwires not redundant assertions, only the downstream event of a cs-to-geste pairing is bounded to the period, `n_*` lst frames are never rendered so an NA no citation reaches is not a defect; plus the two traps that produce wrong measurements (post-`easy_label()` frames are factors so `== 3` returns zero silently, and `collect()` row order is unstable so compare on a unique key), and the false-positive shapes of the 2026-08-10 pass, where 7 of 12 accepted fixes were applied as written and 2 proposed remedies would have introduced defects (severity justified by a mismatched comparator, an intermediate frame reported as the study frame, a symmetric bound where only one side is at risk, harmonising two siblings that differ on purpose, a fix that is a literal no-op). Streamlit annotation app (annot/), from the 2026-08-11 pass: read `.claude/DEFERRED.md` first since a finding may be a known deferral whose stated cost was itself wrong, cross-check the reviewer's output against the design doc's own stated review motivations since covering a target is not covering the concern that motivated it, `st.sidebar.foo()` inside `with st.sidebar.container()` ignores the with-stack, `st.cache_data` hashes argument values so keying a reader on a constant path is a no-op, `AppTest` rewrites the output parquet so back it up, and three more remedy-level false positives (duplicating a study definition into a second language, trading an exact exception for a misleading message, conflating two workflow states in a startup guard)
- [feedback_review_severity_edscrib.md](feedback_review_severity_edscrib.md): Calibrate reviews of the edscrib annotation socle (~/Documents/packages/py-edscrib): do NOT soften the write path, the closure-in-shell composition is the deliberate template, the two instructed blind spots and what their interaction still hides, and what the auth.py pass added (tiers move both ways, a widened validation widens every rerun, Claude's own fix as the worst finding, asserted vs rebuilt type narrowing), and what the second auth.py pass added (narrowing a catch reopens what the broad form held by accident, Claude's repair of its own regression was itself incomplete, enumerate escape classes, availability beats diagnosability on the gate, ouroboros_evaluate is fragile as a gate), and what the second `app.py` pass added (removing two halves of one mechanism leaves a hole neither test covers, sweep the dependency floor before rejecting on library behaviour, a stale .pyc survives a line-moving edit, mtime+size is not a file identity, a guard's remedy can cement a decision nobody made), and what the second `navigation.py` pass added (a load-time guard proves nothing about a write a rerun boundary away, a silent repair launders the evidence a guard waited for, L1 reversed two of Claude's own remedies including one backed by a strawman measurement, dtype parity between readers is correctness since float64 collapses ids past 2^53, an in-repo precedent settles a convention faster than the convention, mutation caught two non-discriminating tests), and what its tail added (a reviewer's framework mechanism is checkable in the vendored bundle, 'removes the trap' can mean 'removes the guard rail', an assertion against a module constant tests nothing, a contract wants both its static and its runtime form), and what the `export.py` pass added (the reviewer's remedy AND L1's both failed on one point, fetch a citation before taking a severity from it, both examples of a finding can be wrong while its mechanism holds, catch_warnings is process-global and unusable here, openpyxl sheet-rename and leading-equals facts, a proto field is testable at the proto), and what the second `export.py` pass added (the blindspot bucket ranking holds on application code with external-only at 0 of 8, two findings on one line can share no defect, a demotion covering one branch of a binding covers neither, an exception message is an egress the module owns, a dtype filter written for the reader in front breaks for the caller behind, measure and the perf finding dies rather than the code, adjudicate a multi-claim docstring finding claim by claim), and what the `config.py` pass added (a finding about a shape is not a finding about where the shape is validated, so four fixes left the reviewed file for `load_frames`; a keyword defeats two identity guards; scope a kw_only recommendation per class by running the suite; prefer a generic walk to an enumeration on a growing shape; a guard reading persisted_fields is blind to the collision that loses an annotation; rejected restructuring `rows` for an unwritten consumer; measure the framework rather than assuming it raises), and what the third `app.py` pass added (justify an escalation on the axis that carries it rather than on the clinical stakes, fixing a diagnosis is not fixing an outage, a guard ordering is proven by mutation and not by prose, L1 found a dedup defect neither the reviewer nor Claude saw), and what the fourth added (a framework primitive lying to the type checker left a docstring convention as the only thing in front of the write, a green test can be green because of the defect you are about to fix, the reviewer measured a different scenario than the code constructs and quoted the tell in its own report, the consumer's parquet schema settled a Required in one command, L1 right in conclusion and wrong in mechanism, the external sweep run inside the wrap-up caught two counts hidden in progression chains), and what the render half added (a callback reads what the framework refreshed and not what the body wrote, so a correction reverted rather than a value lost; L1 beat both the reviewer and Claude on the remedy twice; decode the shipped frontend bundle rather than reasoning about the framework; the consumer's stylesheet is evidence and settled three points; a construction refusal caught four drifted declarations in the repo's own tests; `rg -rn` is --replace and mangled the sweep)
- [feedback_explicit_invocation_gate.md](feedback_explicit_invocation_gate.md): Explicit-only invocation gate for hebstr/claude-code-plugins skills; reject reviewer trigger-breadth findings
- [feedback_review_severity_litrev_mcp.md](feedback_review_severity_litrev_mcp.md): Calibrate adversarial audits on litrev-mcp (FastMCP server): in-memory pipeline ≠ file mutation, reactive env-tips are idiomatic FastMCP, empty-string is the schema sentinel, MCP error dicts are LLM-consumed (no error-code taxonomy), don't tighten regex on user-authored fields (5 recurring false positives, blindspot 2026-05-17)
- [feedback_litrev_pipeline_patterns.md](feedback_litrev_pipeline_patterns.md): Recurring `/litrev` scoping decisions: post-hoc restrict above 150 incl., skip backward snowball with recent window, audit_claims/synthesize DOI conflict, bypass validate_gate stall, strip pseudo-GRADE, two-pass post-gen prose cleanup (6 rules, epidemio PRISE 2026-05-17/18)
- [feedback_walkthrough_process.md](feedback_walkthrough_process.md): Interactive walkthrough: point-by-point, terse, inline corrections, recap at end
- [feedback_verify_quarto_theming.md](feedback_verify_quarto_theming.md): Quarto theming: always render and scan compiled CSS to verify SCSS changes land and win specificity
- [feedback_quarto_render_disabled_chapter.md](feedback_quarto_render_disabled_chapter.md): Rendering a `.qmd` commented out of a book's `chapters:` can make Quarto uncomment it in `_quarto.yml`; check `git status` on the project config before calling the render gate passed
- [feedback_french_prose.md](feedback_french_prose.md): FR prose: authorities, anglicism policy, typography, register: full reference for prose work
- [feedback_review_third_party_content.md](feedback_review_third_party_content.md): Don't apply CLAUDE.md prose style rules to third-party skill descriptions, plugin docs, or forked content
- [feedback_external_pattern_imports.md](feedback_external_pattern_imports.md): Filter for external Claude Code config imports: identify the consumer that justifies the format before importing (4 false positives in solatis audit 2026-05-16)
- [feedback_verify_after_install.md](feedback_verify_after_install.md): Smoke-test new tools/hooks/integrations end-to-end before marking done; never suppress stderr on fresh config
- [feedback_shell_grep_pipefail.md](feedback_shell_grep_pipefail.md): `grep` as filter under `set -Eeuo pipefail` crashes on no-match; idiomatic fixes + audit grep
- [feedback_rg_misses_gitignored_trackers.md](feedback_rg_misses_gitignored_trackers.md): bare `rg` skips hidden and gitignored paths, so a dependency grep reports the `.claude/` trackers as clean; use `--hidden --no-ignore` when asking what references a symbol
- [feedback_line_number_cross_refs.md](feedback_line_number_cross_refs.md): never cite by line number, code targets included (they break silently on any insertion above them); anchor on the symbol name for code, on a section title or verbatim quote for prose, and prove it resolves by literal grep; plus when NOT to renumber a drifted ref (dated or superseded sections are deliberate record, corrected by strikethrough; only refs naming a vanished symbol warrant an edit)
- [feedback_prose-lint-write.md](feedback_prose-lint-write.md): prose hygiene applies to user-facing prose only (which trees are exempt, why the stowed global config is not), soft-wraps belong to panache, typographic vs rhetorical em/en dash distinction, when to invoke /workflow:write
- [feedback_no_bulk_regex_for_contextual_skills.md](feedback_no_bulk_regex_for_contextual_skills.md): Don't substitute a contextual editing skill (workflow:write, audit reviewers) with a bulk regex when per-case Edit is blocked; surface the blocker instead
- [feedback_audit_walkthrough.md](feedback_audit_walkthrough.md): audit:walkthrough triage table format, severity calibration for personal scripts, the calibration loader contract (redirect-follow + scoped-filename glob, and its cwd-vs-target failure mode), the ouroboros_evaluate Stage 1 that purges an editable install and kills L2, and the measured blindspot bucket accuracy (agreed reliable, external-only least reliable, inverting the skill's own framing), which the 2026-07-31 export.py pass showed holds on application code too (0 of 8 external-only accepted), correcting this file's own earlier claim that it did not generalize, plus the rule that a near-miss pair is never collapsed into agreed
- [feedback_format_hook_strips_new_import.md](feedback_format_hook_strips_new_import.md): the format-on-edit hook runs `ruff check --fix`, so a Python import added before its first use is stripped as F401 between two Edits; write the usage first
- [feedback_browser_layout_probe.md](feedback_browser_layout_probe.md): Inspect a rendered layout yourself instead of asking the user: snap chromium runs headless in the sandbox (screenshot, --dump-dom) and --remote-debugging-port plus websockets drives hover and getComputedStyle over CDP; Firefox headless does not start; the injected ancestor probe is the fallback
- [feedback_checkup_scope.md](feedback_checkup_scope.md): "audit complet du répertoire" = scan every file in CWD, not just the active file, minus the NOTES.md / TODO.md read-gate
- [feedback_personal_working_files.md](feedback_personal_working_files.md): rationale and edge cases behind the CLAUDE.md read-gate on NOTES.md / TODO.md (what does not license a read, why editing is the stricter gate)
- [feedback_ouroboros_tools.md](feedback_ouroboros_tools.md): Only `ouroboros_evaluate` supports `trigger_consensus` (`ouroboros_qa` silently ignores it); and `ouroboros_evaluate` reads `working_dir` from disk, so a call still running while the fix lands judges the corrected code and returns a misleading APPROVED
- [feedback_skill_adversary_sync.md](feedback_skill_adversary_sync.md): skill-adversary on workflow:sync: 7 false-positive patterns to pre-filter
- [feedback_xdg_open.md](feedback_xdg_open.md): xdg-open silently fails on file:// in Positron/VSCode; use `python3 -m http.server` for HTML preview
